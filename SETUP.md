# Setup

1. Create/open the `karthickchemist` profile repository.
2. Replace its `README.md` with this build's `README.md`.
3. Copy the `assets/` folder beside it.
4. Commit and push.

The README is self-contained: the visible animated modules are local GIF assets.
